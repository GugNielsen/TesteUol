<?xml version="1.0" encoding="UTF-8"?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 x:Class="TesteUol.Views.DetailsClimaPage"
                 xmlns:forms="clr-namespace:Lottie.Forms;assembly=Lottie.Forms"
                 xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
                 BackgroundImageSource="">

    <StackLayout Margin="18"
                 Padding="36"
                 HorizontalOptions="Center"
                 VerticalOptions="Center"
                 BackgroundColor="#6495ED" 
                 HeightRequest="500"
                 WidthRequest="500">

          <StackLayout Orientation="Vertical">
            <Grid ColumnSpacing="0" RowSpacing="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="100" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--{Binding Date,StringFormat='{0:dd}'}-->
                <!--  header background  -->
                <StackLayout Margin="5"  Orientation="Horizontal" HorizontalOptions="CenterAndExpand" VerticalOptions="StartAndExpand">
               <Label Text="{Binding Date,StringFormat='{0:dd}'}" TextColor="#000" FontSize="34" VerticalOptions="Center"/>
                 <Label Text="{Binding Date,StringFormat='{0:MMMM,dddd}'}"  TextColor="#000" FontSize="12" VerticalOptions="Center"/>
             </StackLayout>

                <StackLayout  Margin="0,40,0,0">
                       <forms:AnimationView
                x:Name="LottieAnimation"
                Grid.Row="0"
                HeightRequest="90"
                WidthRequest="90"
                Animation="{Binding ImageAnimada}"
                AutoPlay="true"
                Loop="True"
                Speed="1"
                Scale="1"
                HorizontalOptions="FillAndExpand"
                VerticalOptions="FillAndExpand" />
                </StackLayout>
            </Grid>
              <!--{Binding TemperaturaMinima}-->
              <!--Binding TemperaturaMaxima}-->
            <!--  Profile Name  -->
            <StackLayout  Margin="30" HorizontalOptions="Center">
                  <StackLayout  Spacing="30" Orientation="Horizontal"  VerticalOptions="EndAndExpand">
               <Label HorizontalOptions="CenterAndExpand" Text="{Binding TemperaturaMinima ,StringFormat='{0:#}°c mim'}" TextColor="#000" FontSize="25" VerticalOptions="Center"/>
                   <Label HorizontalOptions="CenterAndExpand"  Text="{Binding TemperaturaMaxima, StringFormat='{0:#}°c max'}" TextColor="#000" FontSize="25" VerticalOptions="Center"/>
             </StackLayout>

            
            </StackLayout>

                <Label Margin="0" Text="{Binding Summary}" TextColor="White" FontSize="12" HorizontalOptions="Center"/>

            <BoxView Margin="20,10" HeightRequest="1" BackgroundColor="#F1F2F6"/>

              <Frame HasShadow="false"
                     IsClippedToBounds="true"
                     Padding="5"
                     CornerRadius="12"
                     BackgroundColor="#fff">
            <Grid RowSpacing="20" Margin="20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="AUTO"/>
                  
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="AUTO"/>
                    <RowDefinition Height="AUTO"/>
                    <RowDefinition Height="AUTO"/>
                </Grid.RowDefinitions>
                
                <StackLayout Spacing="10" Grid.Column="0" Orientation="Horizontal" HorizontalOptions="StartAndExpand" VerticalOptions="CenterAndExpand" Grid.Row="0" Grid.RowSpan="1"  Grid.ColumnSpan="3">
                <Image Source="humidity.png" WidthRequest="30" HeightRequest="20"/>
                <Label Text="{Binding  Humidity, StringFormat='{0:0}%'}"  TextColor="Black" FontSize="12" FontAttributes="Bold" />
                <Label Text="Humidity" TextColor="#7D7D7D" FontSize="12" />
                </StackLayout >

                  <StackLayout  Spacing="10" Grid.Column="0" Orientation="Horizontal" HorizontalOptions="StartAndExpand" VerticalOptions="CenterAndExpand" Grid.Row="2" Grid.RowSpan="1"  Grid.ColumnSpan="3">
                <Image   Source="wind.png" WidthRequest="30" HeightRequest="20" />
                <Label  Text="{Binding  WindSpeed ,StringFormat='{0:0.0} m/s'}"  TextColor="Black" FontSize="12" FontAttributes="Bold" /> 
                <Label  Text="Wind"  TextColor="#7D7D7D" FontSize="12" />

                </StackLayout >

                   <StackLayout  Spacing="10" Grid.Column="0" Orientation="Horizontal" HorizontalOptions="StartAndExpand" VerticalOptions="CenterAndExpand" Grid.Row="1" Grid.RowSpan="1"  Grid.ColumnSpan="3">
                  <Image  Source="pressure.png" WidthRequest="30" HeightRequest="20"/>
                <Label  Text="{Binding Pressure , StringFormat='{0:0} hpa'}"  TextColor="Black" FontSize="12" FontAttributes="Bold" /> 
               <Label  Text="Pressure"  TextColor="#7D7D7D" FontSize="12" />
                </StackLayout >
              <StackLayout Spacing="10" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalOptions="StartAndExpand" >
                <Image Source="cloudiness.png" WidthRequest="30" HeightRequest="20"/>
                <Label Text="{Binding DewPoint , StringFormat='{0:#}°c'}"  TextColor="Black" FontSize="12" FontAttributes="Bold" />
                <Label Text="Dew Point" TextColor="#7D7D7D" FontSize="12" />
              </StackLayout>


            </Grid>
               </Frame>
            <BoxView Margin="20,10" HeightRequest="1" BackgroundColor="#F1F2F6"/>

        </StackLayout>
      
    </StackLayout>
</pages:PopupPage>
